{
  "name": "queue-socket-provider-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create queue-socket-db",
    "db:migrate": "wrangler d1 migrations apply queue-socket-db",
    "queues:create": "wrangler queues create postback-queue"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.91.0"
  },
  "dependencies": {
    "hono": "^4.6.0"
  }
}
